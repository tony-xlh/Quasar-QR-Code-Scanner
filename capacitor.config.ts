import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.xulihang.quasarqrcodescanner',
  appName: 'quasar-qrcode-scanner',
  webDir: 'dist/spa',
  bundledWebRuntime: false
};

export default config;
